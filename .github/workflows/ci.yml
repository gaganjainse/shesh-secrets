name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Least privilege by default; jobs that write declare it explicitly.
permissions: read-all

jobs:
  component:
    uses: gaganjainse/shesh-ecosystem/.github/workflows/component-ci.yml@main
